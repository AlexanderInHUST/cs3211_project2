//
// Created by 唐艺峰 on 2018/3/24.
//

#ifndef GALAXY_SIMULATION_COMMON_H
#define GALAXY_SIMULATION_COMMON_H

#include <stdlib.h>
#include <time.h>
#include <memory.h>
#include <stdio.h>
#include <math.h>
#include <mpi.h>
#include <sys/time.h>

#define MAX(a, b) ((a) > (b) ? (a) : (b))
#define MIN(a, b) ((a) < (b) ? (a) : (b))

#endif //GALAXY_SIMULATION_COMMON_H
